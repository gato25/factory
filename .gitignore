# Local Claude Code state (may contain tokens or machine-specific permissions).
# The spec-kit skills under .claude/skills/ are shared tooling and stay tracked.
.claude/settings.local.json
.claude/.credentials.json

# pen.dev CLI local state
.pencil/

# Node / Bun
node_modules/
*.log
.env
.env.*
!.env.example

# SvelteKit / Vite build output
.svelte-kit/
build/
dist/

# Test output
coverage/
test-results/
playwright-report/

# OS noise
.DS_Store
Thumbs.db
*.tmp
*.swp
